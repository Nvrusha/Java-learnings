package Oct.Ex18_CollectionFramework.Set;

public class Set_Notes {
    public static void main(String[] args) {

        /*
         Set in Java: A collection that ensures unique elements (no duplicates)
         Key Features of Set:

         1. No Duplicates: Each element in a Set is unique.
         2. Unordered Collection: Does not maintain any specific order (except LinkedHashSet).
         3. Null Allowed: Allows at most one null value.

         Types of Set:

         1. HashSet:
            - Uses hashing for storage.
            - Does not maintain order of elements.
            - Best for fast add, remove, and search operations.

         2. LinkedHashSet:
            - Maintains insertion order.
            - Slightly slower than HashSet but useful when order matters.

         3. TreeSet:
            - Maintains elements in sorted order.
            - Does not allow null values.
            - Slower than HashSet and LinkedHashSet.

         Common Methods of Set:

         1. add(element): Adds an element to the set.
         2. remove(element): Removes the specified element.
         3. contains(element): Checks if the element exists in the set.
         4. size(): Returns the number of elements in the set.
         5. isEmpty(): Checks if the set is empty.
         6. clear(): Removes all elements from the set.
         7. iterator(): Provides an iterator for traversing the set.

         When to Use:

         - Use Set when duplicates are not allowed.
         - Use HashSet for performance (quick lookups).
         - Use TreeSet if sorted data is required.
         - Use LinkedHashSet when preserving insertion order is necessary.
        */

    }
}
